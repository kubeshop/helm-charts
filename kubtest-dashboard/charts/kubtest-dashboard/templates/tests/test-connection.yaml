apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubtest-dashboard.fullname" . }}-test-connection"
  labels:
    {{- include "kubtest-dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubtest-dashboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
