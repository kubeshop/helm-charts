apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cypress-executor.fullname" . }}-test-connection"
  labels:
    {{- include "cypress-executor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cypress-executor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
