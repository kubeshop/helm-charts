apiVersion: v1
kind: Service
metadata:
  name: {{ include "testkube-logs.fullname" . }}
  labels:
    {{- include "testkube-logs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "testkube-logs.selectorLabels" . | nindent 4 }}
