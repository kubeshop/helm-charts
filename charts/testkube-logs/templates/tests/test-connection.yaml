apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "testkube-logs.fullname" . }}-test-connection"
  labels:
    {{- include "testkube-logs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "testkube-logs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
