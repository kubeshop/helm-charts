apiVersion: v1
kind: Service
metadata:
  name: {{ include "curl-executor.fullname" . }}
  labels:
    {{- include "curl-executor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "curl-executor.selectorLabels" . | nindent 4 }}
