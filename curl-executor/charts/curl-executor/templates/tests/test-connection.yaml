apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "curl-executor.fullname" . }}-test-connection"
  labels:
    {{- include "curl-executor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "curl-executor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
