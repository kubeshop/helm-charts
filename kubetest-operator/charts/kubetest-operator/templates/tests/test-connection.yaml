apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubetest-operator.fullname" . }}-test-connection"
  labels:
    {{- include "kubetest-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubetest-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
